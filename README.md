# DataJanitor-LLM
🧹 DataJanitor LLM: Limpeza de Dados Inteligente com StreamlitEste projeto é uma ferramenta robusta e dinâmica desenvolvida em Python (Pandas/Streamlit) que automatiza o processo de limpeza e padronização de conjuntos de dados. Ele foi projetado para simular a capacidade de inferência de um Large Language Model (LLM) ao deduzir o tipo de dado correto e aplicar regras de limpeza em lote, mesmo em dados "sujos" ou inconsistentes.✨ Principais FuncionalidadesInferência Dinâmica de DTypes: Deduz o tipo de dado pretendido (Data, Numérico, Texto) com base no nome da coluna (Ex: 'QUANTIDADE', 'VALOR', 'DT_FIM').Padronização de Texto: Converte todos os valores textuais para minúsculo, remove acentos e caracteres especiais, garantindo consistência (FULANO DA SILVA $\rightarrow$ fulano da silva).Conversão Numérica Robusta: Identifica e converte números escritos por extenso (Ex: "VINTE" $\rightarrow$ 20) e remove sujeiras comuns (moedas, espaços, vírgulas, pontos) para retornar float64 ou Int64.Tratamento de Colunas Mistas (Split Inteligente): Detecta colunas que contêm texto e números misturados (Ex: "Produto 10g") e divide-as automaticamente em duas novas colunas (_TEXTO_Limpo e _NUMERICO_Limpo).Leitura de CSV/Excel: Utiliza lógica robusta para ler arquivos CSV, identificando automaticamente o separador e a codificação, além de suportar arquivos Excel (.xlsx).🛠️ Como ExecutarEste projeto requer o Streamlit e pode ser executado em qualquer ambiente Python (localmente ou em serviços como Google Colab/Streamlit Cloud).Bashpip install streamlit pandas openpyxl unidecode numpy
streamlit run app.py
